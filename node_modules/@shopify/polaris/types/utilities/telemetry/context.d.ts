import React from 'react';
export interface TelemetryObject {
    produce(schemaId: string, payload: Record<string, any>): boolean;
}
export declare const TelemetryContext: React.Context<TelemetryObject | undefined>;
